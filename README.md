# ColorVision-Image-Segmentation-using-K-Means-Clustering
An AI &amp; ML project that segments images using the K-Means clustering algorithm. It groups pixels into color-based clusters to simplify images, detect objects, and highlight key regions. Built from scratch in Python with adjustable clusters (k) for flexible image segmentation.
